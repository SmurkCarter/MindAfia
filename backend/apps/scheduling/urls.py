from django.urls import path

# Minimal placeholder URLConf for scheduling module.
# This allows Django to import the module without errors.

urlpatterns = [
    # Add real scheduling endpoints here when ready.
]
